# 日志系统使用说明

## 功能特性

✅ **自动日志记录** - 所有运行信息、告警、错误都会自动记录到文件
✅ **双重输出** - 同时输出到控制台和日志文件
✅ **自动轮转** - 每小时创建新的日志文件
✅ **自动清理** - 只保留最近24小时的日志
✅ **彩色输出** - 控制台显示彩色日志，便于查看
✅ **异常追踪** - 自动记录完整的异常堆栈信息

## 日志文件位置

所有日志文件保存在 `logs/` 目录下：

```
logs/
├── monitor.log              # 当前日志文件
├── monitor.log.2026-01-05_10  # 历史日志（按小时轮转）
├── monitor.log.2026-01-05_11
└── ...
```

## 日志级别

- **DEBUG** - 调试信息（仅记录到文件，不显示在控制台）
- **INFO** - 普通信息（控制台和文件都记录）
- **WARNING** - 警告信息
- **ERROR** - 错误信息
- **CRITICAL** - 严重错误

## 使用方法

### 1. 运行监控程序（自动记录日志）

```bash
python realtime_monitor_v2.py
```

程序运行时会自动：
- 在控制台显示彩色日志
- 将所有日志写入 `logs/monitor.log`
- 每小时轮转日志文件
- 自动删除超过24小时的旧日志

### 2. 查看历史日志

```bash
python view_logs.py
```

这会显示最近24小时的所有日志，**最新的日志在前面**。

### 3. 手动查看日志文件

直接打开日志文件：

```bash
# Windows
notepad logs\monitor.log

# 或使用任何文本编辑器
code logs\monitor.log
```

## 日志内容示例

```
[2026-01-05 14:30:00] [INFO] ============================================================
[2026-01-05 14:30:00] [INFO] 🔍 实时价格监控器启动
[2026-01-05 14:30:00] [INFO]    采样间隔: 60 秒
[2026-01-05 14:30:00] [INFO]    最大时间窗口: 5 分钟
[2026-01-05 14:30:00] [INFO] ============================================================
[2026-01-05 14:30:01] [INFO] [WS] 正在连接 Binance WebSocket...
[2026-01-05 14:30:02] [INFO] [WS] 连接成功！
[2026-01-05 14:31:00] [INFO] 📊 采样: 245 个币种 | 历史: 1/6 分钟
[2026-01-05 14:32:00] [INFO] 📊 采样: 245 个币种 | 历史: 2/6 分钟
[2026-01-05 14:36:00] [INFO] [ALERT] BTCUSDT: +32.50% - 5分钟暴涨30%
[2026-01-05 14:36:00] [DEBUG] 告警详情: {"upOrDown":"正百分之三十二点五零","symbol":"BTC","currentPrice":"..."}
[2026-01-05 14:40:00] [ERROR] [WS] 连接断开: Connection closed
[2026-01-05 14:40:00] [INFO] [WS] 5秒后重连...
```

## 日志保留策略

- **保留时间**: 24小时
- **轮转周期**: 每小时
- **自动清理**: 启动时自动删除超过24小时的旧日志

## 故障排查

### 日志文件太大？

日志每小时轮转一次，单个文件不会太大。如果需要调整保留时间，修改 `realtime_monitor_v2.py`:

```python
self.logger = Logger(name="monitor", keep_hours=12)  # 改为保留12小时
```

### 找不到日志文件？

确保 `logs/` 目录存在，程序会自动创建。如果没有，手动创建：

```bash
mkdir logs
```

### 日志没有记录？

检查是否有文件写入权限，确保程序有权限在当前目录创建 `logs/` 文件夹。

## 高级配置

### 修改日志级别

编辑 `logger.py` 中的日志级别设置：

```python
# 文件日志级别（记录所有）
file_handler.setLevel(logging.DEBUG)

# 控制台日志级别（只显示重要信息）
console_handler.setLevel(logging.INFO)  # 改为 DEBUG 可以看到更多信息
```

### 禁用彩色输出

如果终端不支持彩色，可以禁用：

```python
console_formatter = CustomFormatter(use_color=False)
```

## 注意事项

⚠️ **日志文件包含敏感信息** - 日志中可能包含价格、告警等信息，请妥善保管
⚠️ **磁盘空间** - 24小时的日志通常占用几MB到几十MB，确保有足够空间
⚠️ **编码问题** - 日志使用UTF-8编码，确保文本编辑器支持UTF-8
